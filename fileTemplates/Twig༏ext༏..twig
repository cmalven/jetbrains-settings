{# =============================================================== #}
{# ${StringUtils.removeAndHump(${NAME}, ".")})
{# =============================================================== #}
{#
  {% include '_partials/${NAME}' %}
#}